// eslint-disable-next-line react/prop-types
const LandingPage = ({ children }) => {
  return (
    <div style={{ height: '100vh', background: 'lightblue' }}>{children}</div>
  );
};

export default LandingPage;
